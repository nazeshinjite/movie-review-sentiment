# AI-use disclosure log

AAI-501 permits generative-AI use **with disclosure.** This log records what AI tools did, so the paper's disclosure statement and APA citation are accurate. Log every non-trivial use: code generation, drafting, debugging, analysis. The LLM-as-judge (notebook 05) is a *method*, not an authoring aid — describe it in Methods; still note here which model/endpoint adjudicated.

## How to log

One row per use: date, member, tool + model, what it did, and how the output was verified (the master's-level bar is that a human understands and can defend every result).

| Date | Member | Tool / model | What it did | Verification |
|---|---|---|---|---|
| 2026-07-19 | Ian Schmitt | Claude Code (Claude Opus 4.8) | Scaffolded the repository structure, READMEs, and environment config (this framing step). No analysis or model code generated. | Structure reviewed and approved by the repo owner before commit. |
| 2026-07-23 | Ian Schmitt | Claude Code | Assisted in building notebook 01 (EDA): cell scaffolding, matplotlib plotting code, and the smoothed log-ratio distinctiveness statistic, from an analysis plan I wrote. Interpretation and the takeaways are mine. | Notebook re-executed from a clean kernel; every numeric claim in the takeaways recomputed against `splits.parquet` independently of the notebook before commit. |
| 2026-07-24 | Ian Schmitt | Claude Code (Claude Opus 5) | Reviewed teammate PR #1 (00_core + shared.py) by executing it rather than reading the diff, which surfaced a split-construction defect; after the fix, re-verified the artifacts and approved. Then ran the bundled `notebook-reviewer` agent over notebook 01 pre-PR. | Split correctness confirmed by rebuilding it independently from the same seed and comparing row by row. Every reviewer finding re-derived by hand before acting: the flagged markup claim was an overlap fallacy, and recomputing the disjoint count gave 2 regex false positives and 0 real HTML tags. Corrections applied and the notebook re-executed. |

| 2026-07-25 | Ian Schmitt | Codex CLI (GPT-5.6) | Ran an adversarial review over notebook 01, prompted to challenge design choices rather than hunt for defects. It raised four findings. | Each one re-derived by hand before acting. Two were correct and were fixed (outputs generated against a non-canonical artifact; a duplicate audit blind to cross-split leakage). Two were correct in principle but overstated in severity, and their prescriptions were declined with reasons recorded in the commit: duplicating 00's schema validation inside 01, and regenerating the splits to remove benchmark duplicates, which would mutate a frozen team decision for a 0.19% effect. |
| 2026-07-25 | Ian Schmitt | Claude Code (Claude Opus 5) | Assisted with the resulting 01 changes, the two-round review of teammate notebook 02, and the `outputs/` naming convention. | Every quantitative claim measured before it was asserted: the markup inflation on word counts, the length-only ROC-AUC, the cross-split duplicate counts, and the regularisation effect in 02's ablation were each computed directly and independently of the notebook that reports them. Reviewer suggestions were accepted or declined on measured evidence, not deference — a proposed log-x histogram was rejected after rendering it and finding it hid the outliers it was meant to reveal. |
| 2026-08-01 | Ian Schmitt | Claude Code (Claude Opus 5) | Two-round review of teammate PR #6 (notebook 03): executed the committed model and predictions rather than reading the diff, which surfaced that the committed `.keras` did not reproduce the committed predictions on this machine. | The mismatch was measured, not inferred: both artifacts loaded and re-scored against the canonical validation split, metrics compared to the notebook's committed outputs. |
| 2026-08-02 | Ian Schmitt | Claude Code (Claude Fable 5) | Root-caused the cross-machine model failure: basis-vector probes of the trained network, a tie-break flip simulation, and verification against the teammate's shipped artifacts isolated sklearn's `max_features` tie instability as the cause (197 substituted vocabulary terms between machines). Prepared the canonicalization change: committed vectorizer + fingerprint asserts in `shared.py` and notebook 00. | Every claim measured twice. The probe method was validated on a control model first (100/100 sign agreement); the teammate's vectorizer reproduced her committed predictions to 1.5e-07; all 394 differing terms confirmed to sit exactly at the tie count (19); IDF on shared terms byte-identical; the flip simulation bracketed the observed accuracy collapse. Both the verify and the reject paths of the new fingerprint assert were exercised before commit. |

| 2026-07-31 | Keana Gindlesperger | Claude (Claude Sonnet 5, claude.ai) | Asked Claude to review notebook 03 (neural network) for code quality, comment clarity, and any improvements. It flagged a real defect — a local absolute path and Windows username leaking into two committed output cells (the freeze/save manifest print and a stray TensorFlow warning in the tuning-loop cell) — and fixed the save-manifest prints to use `PATHS["repo_root"].relative_to()`, matching the convention notebook 00 already follows. No model architecture, tuning grid, or metrics logic was touched. | Diffed the notebook JSON before/after line by line to confirm only the flagged path-leak lines changed. Notebook restarted and re-run top-to-bottom to regenerate clean outputs before commit. |

| 2026-07-31 | Yesid Cardenas Marin | Gemini  | Assisted notebook 02 Logistic Regression with a guided feature matrix inspection, baseline Logistic Regression model setup, evaluation metric computation, and standardization of prediction artifact exports (`02-lr_val.parquet`).. Successfully implemented Notebook 02 pipeline, saved trained model artifact (`logreg.joblib`), and ensured validation reproducibility across environment runs. |
| 2026-08-02 | Yesid Cardenas Marin | Gemini  | Assisted notebook 04 Evaluation with resolving evaluation code errors, configuring visualization labels (`["Negative", "Positive"]`), implementing model reproduction sanity assertions (`assert np.allclose`). Successfully created, updated, and verified PR #9 with full test evaluation metrics, figures, and handoff artifacts. |

## For the paper

The disclosure statement summarizes this log. Cite each AI tool in APA 7 (e.g., the LLM-judge model and any drafting/coding assistant). Keep model names and versions accurate — `[VERIFY]` anything uncertain before submission.
